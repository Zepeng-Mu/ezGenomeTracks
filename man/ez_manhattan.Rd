% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ez_manhattan.R
\name{ez_manhattan}
\alias{ez_manhattan}
\title{Easy genome-wide Manhattan plot visualization}
\usage{
ez_manhattan(
  input,
  chr = NULL,
  bp = NULL,
  p = NULL,
  snp = NULL,
  track_labels = NULL,
  group_var = NULL,
  logp = TRUE,
  size = 0.5,
  colors = c("grey", "skyblue"),
  highlight_snps = NULL,
  highlight_color = "purple",
  threshold_p = NULL,
  threshold_color = "red",
  threshold_linetype = 2,
  facet_label_position = c("top", "left"),
  border = FALSE,
  region = NULL,
  gene = NULL,
  ...
)
}
\arguments{
\item{input}{A data frame or named list of data frames containing GWAS results
with columns for chromosome, position, p-values, and optionally SNP names.
Must contain data from multiple chromosomes. Supports both GWAS-style
(CHR, BP, P) and GRanges-style (seqnames, start, pvalue) column naming.}

\item{chr}{Character string specifying the column name for chromosome numbers.
Default: auto-detect from "CHR", "seqnames", "chrom", etc.}

\item{bp}{Character string specifying the column name for base pair positions.
Default: auto-detect from "BP", "start", "pos", etc.}

\item{p}{Character string specifying the column name for p-values.
Default: auto-detect from "P", "pvalue", "p.value", etc.}

\item{snp}{Character string specifying the column name for SNP identifiers.
Default: auto-detect from "SNP", "rsid", "variant_id", etc.}

\item{track_labels}{Optional vector of track labels (used for unnamed list input).
Default: NULL.}

\item{group_var}{Column name for grouping data within a single data frame.
Default: NULL.}

\item{logp}{Logical indicating whether to plot -log10(p-values).
Default: TRUE.}

\item{size}{Numeric value for point size in the plot. Default: 0.5.}

\item{colors}{Vector of colors for alternating chromosomes.
Default: \code{c("grey", "skyblue")}.}

\item{highlight_snps}{Character vector of SNP IDs to highlight.
Default: NULL.}

\item{highlight_color}{Color for highlighting significant SNPs.
Default: "purple".}

\item{threshold_p}{Numeric p-value threshold for drawing a significance line
(e.g., 5e-8 for genome-wide significance). If NULL, no line is drawn.
Default: NULL.}

\item{threshold_color}{Color for the significance threshold line.
Default: "red".}

\item{threshold_linetype}{Linetype for the significance threshold line.
Default: 2 (dashed).}

\item{facet_label_position}{Position of facet labels for multi-track plots:
"top" or "left". Default: "top".}

\item{border}{Logical. If TRUE, adds a black border around the plot panel. Default: FALSE}

\item{region}{Deprecated. Use \code{ez_locusZoom()} for regional plots.}

\item{gene}{Deprecated. Use \code{ez_locusZoom()} for regional plots.}

\item{...}{Additional arguments passed to \code{geom_manhattan()}.}
}
\value{
A ggplot2 object containing the Manhattan plot.
}
\description{
This function creates a Manhattan plot from GWAS (Genome-Wide Association Study)
data, visualizing p-values across the entire genome with chromosomes displayed
along the x-axis using cumulative positions and alternating colors.

For regional association plots (LocusZoom-style) focused on a single locus,
use \code{ez_locusZoom()} instead.
}
\details{
This function creates a genome-wide Manhattan plot for GWAS results across
multiple chromosomes. It is a wrapper around geom_manhattan that provides
a flexible interface with support for grouping and multiple tracks.

The function creates a genome-wide Manhattan plot with:
\itemize{
\item Chromosomes displayed along the x-axis with cumulative positions
\item Alternating colors for adjacent chromosomes (customizable via \code{colors})
\item -log10(p-values) on the y-axis
\item Optional significance threshold line
\item Optional SNP highlighting
}

For multiple tracks (via named list), plots are stacked vertically using facets.
For grouped data (via group_var), colors distinguish different groups.
}
\section{Regional plots}{

For LocusZoom-style regional association plots with LD coloring, gene lookup,
and stackability with other tracks, use \code{ez_locusZoom()} instead.
}

\examples{
# Basic genome-wide Manhattan plot
df <- data.frame(
  CHR = rep(1:3, each = 20),
  BP = rep(1:20, 3) * 1000,
  P = runif(60, 0.0001, 1),
  SNP = paste0("rs", 1:60)
)
ez_manhattan(df)

# With alternating chromosome colors
ez_manhattan(df, colors = c("navy", "orange"))

# With significance threshold
ez_manhattan(df, threshold_p = 5e-8)
}
\seealso{
\code{\link{ez_locusZoom}} for regional association plots,
\code{\link{geom_manhattan}} for the underlying geom
}
