% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_sequence.R
\docType{data}
\name{geom_sequence}
\alias{geom_sequence}
\alias{GeomSequence}
\title{Geom for nucleotide sequence tracks}
\usage{
geom_sequence(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  style = c("text", "tile"),
  show_labels = TRUE,
  label_size = 3,
  label_color = "white",
  tile_height = 0.8,
  tile_width = 1,
  na.rm = TRUE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Aesthetic mappings. Required: \code{x} (genomic position),
\code{label} (nucleotide character). Optional: \code{fill} (nucleotide color),
\code{colour} (tile border color, only used when \code{style = "tile"}).}

\item{data}{Dataset (default: NULL).}

\item{stat}{Statistic to use (default: \code{"identity"}).}

\item{position}{Position adjustment (default: \code{"identity"}).}

\item{...}{Other arguments passed on to \code{\link[ggplot2:layer]{ggplot2::layer()}}.}

\item{style}{Character. Visual style for the sequence track:
\itemize{
\item \code{"text"} (default): bold, colored nucleotide letters with no background
or border. Letter color is taken from the \code{fill} aesthetic.
\item \code{"tile"}: colored background tiles with letters on top (controlled by
\code{show_labels} and \code{label_color}).
}}

\item{show_labels}{Logical. Whether to draw nucleotide letter labels.
For \code{style = "text"}, labels are always drawn when \code{show_labels = TRUE}.
For \code{style = "tile"}, labels are drawn on top of the colored tiles.
Default: \code{TRUE}.}

\item{label_size}{Numeric. Font size for nucleotide letters (in pt).
Default: \code{3}.}

\item{label_color}{Character. Color of the nucleotide letter text, used
only when \code{style = "tile"}. For \code{style = "text"}, letter color is taken
from the \code{fill} aesthetic. Default: \code{"white"}.}

\item{tile_height}{Numeric (0–1). Height of each nucleotide tile as a
proportion of the y-axis range. Only used when \code{style = "tile"}.
Default: \code{0.8}.}

\item{tile_width}{Numeric. Width of each nucleotide tile in data (genomic)
units. Only used when \code{style = "tile"}. Default: \code{1} (one base pair per tile).}

\item{na.rm}{Logical. Silently remove \code{NA} values. Default: \code{TRUE}.}

\item{show.legend}{Logical. Should this layer be included in the legends? \code{NA}, the default,
includes if any aesthetics are mapped. \code{FALSE} never includes, and \code{TRUE}
always includes. It can also be a named logical vector to finely select the
aesthetics to display. To include legend keys for all levels, even when no
data exists, use \code{TRUE}.  If \code{NA}, all levels are shown in legend, but
unobserved levels are omitted.}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics, rather than combining with
them. This is most useful for helper functions that define both data and
aesthetics and shouldn't inherit behaviour from the default plot
specification, e.g. \code{\link[ggplot2:annotation_borders]{annotation_borders()}}.}
}
\value{
A ggplot2 layer.
}
\description{
Renders a nucleotide sequence track using one of two visual styles.
Colors follow UCSC Genome Browser conventions by default
(A = green, C = blue, G = gold, T = red, N = grey).
}
\examples{
\dontrun{
library(ggplot2)
df <- data.frame(
  position   = 1:10,
  nucleotide = c("A", "T", "G", "C", "A", "T", "G", "C", "N", "A"),
  fill       = c("#00A800", "#CC0000", "#CC9900", "#0000CC",
                 "#00A800", "#CC0000", "#CC9900", "#0000CC", "#999999", "#00A800")
)
ggplot(df, aes(x = position, label = nucleotide, fill = fill)) +
  geom_sequence() +
  ggplot2::scale_fill_identity()
}
}
\keyword{datasets}
