% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_feature.R
\docType{data}
\name{geom_feature}
\alias{geom_feature}
\alias{GeomFeature}
\title{Geom for genomic feature tracks}
\usage{
geom_feature(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  height = 0.8,
  color = "#05b1d3",
  fill = "#05b1d3",
  alpha = 0.7,
  na.rm = TRUE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Aesthetic mappings (default: NULL). xmin, xmax are required.}

\item{data}{Dataset (default: NULL)}

\item{stat}{Statistic to use (default: "identity")}

\item{position}{Position adjustment (default: "identity")}

\item{...}{Other arguments passed on to \code{\link[ggplot2]{layer}}. These are
often aesthetics, used to set an aesthetic to a fixed value.}

\item{height}{Height of the features (default: 0.8)}

\item{color}{Border color of the features (default: "#05b1d3")}

\item{fill}{Fill color of the features (default: "#05b1d3")}

\item{alpha}{Transparency (default: 0.7)}

\item{na.rm}{If \code{TRUE}, silently removes \code{NA} values.}

\item{show.legend}{logical. Should this layer be included in the legends?
\code{NA}, the default, includes if any aesthetics are mapped.
\code{FALSE} never includes, and \code{TRUE} always includes.
It can also be a named logical vector to finely select the aesthetics to
display. To include legend keys for all levels, even
when no data exists, use \code{TRUE}.  If \code{NA}, all levels are shown in legend,
but unobserved levels are omitted.}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics,
rather than combining with them. This is most useful for helper functions
that define both data and aesthetics and shouldn't inherit behaviour from
the default plot specification, e.g. \code{\link[ggplot2:annotation_borders]{annotation_borders()}}.}
}
\value{
A ggplot2 layer
}
\description{
This function creates a geom for genomic feature tracks, such as ChIP-seq features,
ATAC-seq features, or any interval-based features. It displays the features as rectangles.
}
\examples{
\dontrun{
library(ggplot2)
p <- ggplot(feature_data) +
  geom_feature(aes(xmin = start, xmax = end, fill = score))
}
}
\keyword{datasets}
