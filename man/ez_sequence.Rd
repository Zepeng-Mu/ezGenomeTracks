% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ez_sequence.R
\name{ez_sequence}
\alias{ez_sequence}
\title{Easy nucleotide sequence track visualization}
\usage{
ez_sequence(
  genome,
  region = NULL,
  gene = NULL,
  gene_db = NULL,
  org_db = NULL,
  extend = 0.1,
  extend_type = c("proportion", "bp"),
  colors = NULL,
  style = c("text", "tile"),
  show_labels = "auto",
  max_label_bp = 200L,
  label_size = 3,
  label_color = "white",
  tile_height = 0.8,
  border = FALSE,
  ...
)
}
\arguments{
\item{genome}{A BSgenome object providing the reference genome sequence
(e.g., \code{BSgenome.Hsapiens.UCSC.hg38::Hsapiens}). The corresponding
BSgenome data package must be installed.}

\item{region}{Genomic region to display, as a character string in the format
\code{"chr:start-end"} (e.g., \code{"chr1:1000000-1000050"}). Either \code{region} or
\code{gene} (with \code{gene_db}) must be provided.}

\item{gene}{Gene name/symbol to look up (e.g., \code{"PTPRC"}, \code{"TP53"}). When
provided, the region is determined automatically from the gene coordinates
in \code{gene_db}. Either \code{region} or \code{gene} must be provided.}

\item{gene_db}{TxDb object for gene coordinate lookup when using \code{gene}.}

\item{org_db}{Optional OrgDb object for gene symbol mapping. When \code{NULL}
(default), the function auto-detects available OrgDb packages.}

\item{extend}{Numeric. Amount to extend the region beyond the gene body when
using \code{gene}. Default: \code{0.1} (10\% of gene length on each side).}

\item{extend_type}{How to interpret \code{extend}: \code{"proportion"} (relative to
gene length) or \code{"bp"} (absolute base pairs). Default: \code{"proportion"}.}

\item{colors}{Optional named character vector of color overrides for
individual nucleotides. Only the specified bases are overridden; others
retain UCSC defaults. Example: \code{c(A = "purple", T = "pink")}.}

\item{style}{Character. Visual style for the sequence track:
\itemize{
\item \code{"text"} (default): bold, colored nucleotide letters with no background
or border. Letter color is derived from \code{colors} / UCSC defaults.
\item \code{"tile"}: colored background tiles with optional letter labels on top.
}}

\item{show_labels}{Logical or \code{"auto"}. Whether to draw nucleotide letters.
When \code{"auto"} (default), labels are shown only when the region width is ≤
\code{max_label_bp}. For \code{style = "text"}, hiding labels (\code{FALSE}) renders an
empty panel; consider switching to \code{style = "tile"} for wide regions.}

\item{max_label_bp}{Integer. Maximum region width (in base pairs) at which
\code{show_labels = "auto"} will display nucleotide letters. Default: \code{200}.}

\item{label_size}{Numeric. Font size for nucleotide letters (in pt).
Default: \code{3}.}

\item{label_color}{Character. Color of nucleotide letters when
\code{style = "tile"}. Ignored for \code{style = "text"} (color comes from the
nucleotide palette). Default: \code{"white"}.}

\item{tile_height}{Numeric (0–1). Height of each nucleotide tile as a
proportion of the panel height. Only used when \code{style = "tile"}.
Default: \code{0.8}.}

\item{border}{Logical. If TRUE, adds a black border around the plot panel. Default: FALSE}

\item{...}{Additional arguments passed to \code{geom_sequence()}.}
}
\value{
A ggplot2 object representing the sequence track, compatible with
\code{vstack_plot()} for stacking with other tracks.
}
\description{
Creates a nucleotide sequence track for a given genomic region, fetching the
DNA sequence from a BSgenome object. Each nucleotide is rendered as a colored
tile following UCSC Genome Browser conventions (A = green, C = blue,
G = gold, T = red, N = grey). Single-letter labels are drawn on the tiles
when the region is narrow enough to display them legibly.
}
\details{
The BSgenome data package for your organism of interest must be installed
separately. For example:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{BiocManager::install("BSgenome.Hsapiens.UCSC.hg38")
library(BSgenome.Hsapiens.UCSC.hg38)
ez_sequence(Hsapiens, region = "chr1:1000000-1000050")
}\if{html}{\out{</div>}}

UCSC nucleotide color defaults can be inspected via \code{ez_sequence_palette()}.
}
\examples{
\dontrun{
library(BSgenome.Hsapiens.UCSC.hg38)

# Default style: bold colored letters, no background
ez_sequence(Hsapiens, region = "chr1:1000000-1000030")

# Tile style: colored background tiles with white labels
ez_sequence(Hsapiens, region = "chr1:1000000-1000030", style = "tile")

# Force no labels (renders empty panel for text style)
ez_sequence(Hsapiens, region = "chr1:1000000-1000030", show_labels = FALSE)

# Override colors
ez_sequence(
  Hsapiens,
  region = "chr1:1000000-1000030",
  colors = c(A = "purple", T = "pink")
)

# Stack with a coverage track
library(aplot)
cov <- ez_coverage(my_bw, region = "chr1:1000000-1000100")
seq <- ez_sequence(Hsapiens, region = "chr1:1000000-1000100")
vstack_plot(list(cov, seq), heights = c(3, 1))
}
}
