% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genome_plot.R
\name{add_highlight}
\alias{add_highlight}
\title{Add highlight regions to a genome track}
\usage{
add_highlight(plot, regions, fill = "yellow", alpha = 0.3, color = NA, ...)
}
\arguments{
\item{plot}{A ggplot2 object representing a genome track}

\item{regions}{Genomic regions to highlight. Can be:
\itemize{
\item A character string specifying a single region (e.g., "chr1:1000000-2000000")
\item A character vector of multiple regions
\item A GRanges object with genomic intervals
\item A data frame with columns: seqnames, start, end (and optionally score, group, etc.)
}}

\item{fill}{Fill color of the highlights (default: "yellow")}

\item{alpha}{Transparency of the highlights (default: 0.3)}

\item{color}{Border color of the highlights (default: NA for no border)}

\item{...}{Additional arguments passed to geom_rect}
}
\value{
A ggplot2 object with the highlight regions added
}
\description{
This function adds rectangular highlights to a genome track to emphasize specific regions.
It supports multiple input formats and can highlight multiple regions simultaneously.
}
\examples{
\dontrun{
# Single region from string
track <- ez_coverage("signal.bw", "chr1:1000000-2000000")
track <- add_highlight(track, "chr1:1200000-1400000")

# Multiple regions from vector
track <- add_highlight(track, c("chr1:1200000-1400000", "chr1:1600000-1800000"))

# From data frame
regions <- data.frame(
  seqnames = "chr1",
  start = c(1200000, 1600000),
  end = c(1400000, 1800000)
)
track <- add_highlight(track, regions)

# From GRanges object
library(GenomicRanges)
gr <- GRanges("chr1", IRanges(c(1200000, 1600000), c(1400000, 1800000)))
track <- add_highlight(track, gr)

# Custom styling
track <- add_highlight(
  track,
  "chr1:1200000-1400000",
  fill = "red",
  alpha = 0.5,
  color = "darkred"
)
}
}
