% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_averaging.R
\name{average_coverage}
\alias{average_coverage}
\title{Average coverage across multiple samples on a common binned grid}
\usage{
average_coverage(
  inputs,
  region,
  n_bins = 2000L,
  summary_fun = c("mean", "median", "max", "min", "sum"),
  nans_to_zeros = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{inputs}{A character vector of BigWig file paths, or a list of data
frames each with columns \code{seqnames}, \code{start}, \code{end}, \code{score}.}

\item{region}{A genomic region string ("chr:start-end") or a GRanges object.}

\item{n_bins}{Number of equal-width bins to tile the region with (default: 2000).}

\item{summary_fun}{Summary function applied across samples per bin. One of
\code{"mean"}, \code{"median"}, \code{"max"}, \code{"min"}, \code{"sum"} (default: \code{"mean"}).}

\item{nans_to_zeros}{Logical. Replace NA/NaN per-bin scores with 0 before
summarizing (default: TRUE).}

\item{verbose}{Logical. If \code{TRUE}, print megadepth progress output when
querying BigWig files. If \code{FALSE} (default), suppress routine messages.}
}
\value{
A data frame with columns \code{seqnames}, \code{start}, \code{end}, \code{score}.
}
\description{
Tiles the specified region into \code{n_bins} equal-width bins, queries each input
at those bins, then summarizes (e.g. mean) across all samples. Because every
sample is queried on the \strong{same} bin grid, no re-alignment is needed.
}
\details{
For BigWig inputs, megadepth is used directly via \code{\link[=.query_megadepth_bins]{.query_megadepth_bins()}}.
For data frame inputs, an overlap-based weighted average is calculated.
}
\examples{
\dontrun{
# Average two BigWig files
avg_df <- average_coverage(
  c("sample1.bw", "sample2.bw"),
  region = "chr1:1000000-2000000",
  n_bins = 1000
)

# Use with ez_coverage for plotting
ez_coverage(avg_df, "chr1:1000000-2000000")

# Average data frames
avg_df <- average_coverage(
  list(df1, df2, df3),
  region = "chr1:1000000-2000000",
  summary_fun = "median"
)
}
}
