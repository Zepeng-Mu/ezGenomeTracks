% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ez_gene.R
\name{ez_gene}
\alias{ez_gene}
\title{Easy gene track visualization}
\usage{
ez_gene(
  input,
  region = NULL,
  gene = NULL,
  gene_db = NULL,
  org_db = NULL,
  extend = 0.1,
  extend_type = c("proportion", "bp"),
  exon_height = 0.2,
  intron_width = 0.6,
  exon_color = NULL,
  exon_fill = NULL,
  intron_color = NULL,
  gene_id = "gene_id",
  gene_name = "gene_name",
  y = "strand",
  label = "gene_name",
  label_size = 3,
  label_color = NULL,
  label_style = c("auto", "simple", "repel", "none"),
  max_labels = NULL,
  label_priority = "length",
  repel_args = list(),
  border = FALSE,
  label_chr = TRUE,
  auto_import = TRUE,
  ...
)
}
\arguments{
\item{input}{Input data source, which can be:
\itemize{
\item A file path to a GTF/GFF file
\item A TxDb object from the GenomicFeatures package
\item A data frame with gene annotation data
}}

\item{region}{Genomic region to display in the format "chr:start-end".
Example: "chr1:1000000-2000000". Either \code{region} or \code{gene} (with \code{gene_db})
must be provided.}

\item{gene}{Gene name/symbol to look up (e.g., "PTPRC", "TP53").
When provided, the region is automatically determined from the gene coordinates.
If \code{gene_db} is not specified, \code{input} is used for coordinate lookup (recommended).
Either \code{region} or \code{gene} must be provided.}

\item{gene_db}{Optional. TxDb object, GTF/GFF file path, or data frame for gene coordinate
lookup when using \code{gene} parameter. If NULL (default), uses \code{input} for lookup.
Only required if you want to use a different annotation source than \code{input}.}

\item{org_db}{Optional OrgDb object for gene symbol mapping. If NULL (default),
auto-detects available OrgDb packages.}

\item{extend}{Numeric. Amount to extend the region beyond the gene body when
using \code{gene} parameter. Default: 0.1 (10\% of gene length on each side).}

\item{extend_type}{How to interpret \code{extend}: "proportion" (relative to gene
length) or "bp" (absolute base pairs). Default: "proportion".}

\item{exon_height}{Relative height of exons (0 to 1). Default: 0.2}

\item{intron_width}{Line width for introns. Default: 0.6}

\item{exon_color}{Border color for exons. Default: NULL (uses strand-based colors
when \code{y = "strand"}, otherwise "gray50")}

\item{exon_fill}{Fill color for exons. Default: NULL (uses strand-based colors
when \code{y = "strand"}, otherwise "gray50")}

\item{intron_color}{Color for intron lines. Default: NULL (uses strand-based colors
when \code{y = "strand"}, otherwise "gray50")}

\item{gene_id}{Column name for gene identifiers. Default: "gene_id"}

\item{gene_name}{Column name for gene symbols/names. Default: "gene_name"}

\item{y}{Column name for the y-axis grouping variable. Default: "strand"}

\item{label}{Column name to use for text labels. If NULL (default), no labels
are displayed. Set to a column name (e.g., "gene_name") to show labels.}

\item{label_size}{Size of text labels. Default: 3}

\item{label_color}{Color of text labels. If NULL (default), uses strand-based
colors when \code{y = "strand"}, otherwise uses exon_fill color.}

\item{label_style}{Strategy for handling overlapping labels. Options:
\itemize{
\item "auto" (default): Uses ggrepel if available, otherwise check_overlap
\item "simple": Standard geom_text with no overlap handling
\item "repel": Force use of ggrepel (errors if not installed)
\item "none": No labels displayed
}}

\item{max_labels}{Maximum number of labels to display. NULL (default) shows all.
When set, labels are filtered based on label_priority.}

\item{label_priority}{Priority criterion for filtering labels when max_labels is set.
Options: "length" (default, prioritizes longer genes), "name" (alphabetical),
or a column name in the input to sort by.}

\item{repel_args}{Named list of additional arguments passed to geom_text_repel()
when label_style = "repel" or "auto" (with ggrepel installed). Default behavior
uses horizontal-only repositioning (\code{direction = "x"}) with no connecting lines
(\code{segment.color = NA}). To show connecting lines, use \code{list(segment.color = "gray50")}.
Override direction with \code{list(direction = "both")} for vertical repositioning too.
Other useful options: \code{max.overlaps}, \code{force}, \code{box.padding}, \code{point.padding}.}

\item{border}{Logical. If TRUE, adds a black border around the plot panel.
Default: FALSE}

\item{label_chr}{Logical. If \code{TRUE} (default), labels the x-axis with the chromosome name
(e.g., "Chr1"). Set to \code{FALSE} to suppress the x-axis label.}

\item{auto_import}{Logical. If TRUE (default), automatically imports GTF/GFF files
passed as character file paths. Set to FALSE to disable auto-import and treat
character input as a literal file path for rtracklayer import. Default: TRUE}

\item{...}{Additional arguments passed to \code{geom_gene()}. Note that \code{color}
and \code{colour} arguments are ignored; use \code{exon_color}, \code{exon_fill}, and
\code{intron_color} instead.}
}
\value{
A ggplot2 object representing the gene track.
}
\description{
This function creates a gene track visualization from genomic annotations,
supporting various input formats including GTF/GFF files, TxDb objects, and
data frames. It automatically handles gene structure visualization with
exons, introns, and strand information.

By default, when \code{y = "strand"}, genes are colored by strand: plus strand
uses \code{"darkgreen"} and minus strand uses \code{"orange2"}. To use uniform colors
instead, explicitly set \code{exon_color}, \code{exon_fill}, and \code{intron_color}.
}
\details{
The function automatically processes different input types:
\itemize{
\item For GTF/GFF files: Uses rtracklayer to import and process the input
\item For TxDb objects: Extracts gene models using GenomicFeatures
\item For data frames: Expects columns for chromosome, start, end, strand, and type
}

The visualization includes:
\itemize{
\item Exons as filled rectangles
\item Introns as connecting lines
\item Strand information with arrowheads
\item Automatic y-axis separation by the specified y variable
}

\strong{Label Overlap Handling:}

The function provides flexible strategies for managing overlapping gene labels:
\itemize{
\item \code{label_style = "auto"}: Automatically uses ggrepel if installed, otherwise
applies check_overlap to hide overlapping labels
\item \code{label_style = "simple"}: Standard text labels with no overlap handling
\item \code{label_style = "repel"}: Uses ggrepel to reposition labels. By default, labels
are repositioned \strong{horizontally only} (\code{direction = "x"}) with no connecting
lines (\code{segment.color = NA}) to maintain a clean appearance while keeping
labels horizontally aligned. This can be changed via \code{repel_args}.
\item \code{label_style = "none"}: Disables all labels
}

When many genes are present, use \code{max_labels} to limit the number of labels shown,
prioritized by \code{label_priority} (gene length by default).
}
\examples{
# From a data frame
data(example_genes)
ez_gene(example_genes, "chr1:11869-14409")

# Limit labels to top 5 longest genes
ez_gene(example_genes, "chr1:42100000-42700000", max_labels = 5)

# Use ggrepel for smart label positioning (if installed)
\dontrun{
# Default: horizontal-only repositioning, no connecting lines
ez_gene(example_genes, "chr1:42100000-42700000", label_style = "repel")

# Show connecting lines to original position
ez_gene(example_genes, "chr1:42100000-42700000",
        label_style = "repel",
        repel_args = list(segment.color = "gray50"))

# Allow both horizontal and vertical repositioning
ez_gene(example_genes, "chr1:42100000-42700000",
        label_style = "repel",
        repel_args = list(direction = "both"))

# Custom repel settings for denser regions
ez_gene(example_genes, "chr1:42100000-42700000",
        label_style = "repel",
        repel_args = list(max.overlaps = 30, force = 3))
}

# Hide overlapping labels automatically
ez_gene(example_genes, "chr1:42100000-42700000", label_style = "auto")

# No labels
ez_gene(example_genes, "chr1:11869-14409", label_style = "none")

\dontrun{
# Using gene name for region lookup
library(TxDb.Hsapiens.UCSC.hg38.knownGene)
txdb <- TxDb.Hsapiens.UCSC.hg38.knownGene
ez_gene(txdb, gene = "PTPRC", gene_db = txdb)
}
}
