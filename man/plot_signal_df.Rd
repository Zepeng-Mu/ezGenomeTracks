% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_helpers.R
\name{plot_signal_df}
\alias{plot_signal_df}
\title{Plot signal tracks from a data frame with grouping}
\usage{
plot_signal_df(
  data,
  region,
  track_by = NULL,
  group_by = NULL,
  color_by = NULL,
  colors = NULL,
  type = "area",
  alpha = 0.8,
  binwidth = NULL,
  facet_scales = "free_y",
  ...
)
}
\arguments{
\item{data}{A data frame containing signal data with columns: start, end, score,
track_by (optional), and group_by (optional)}

\item{region}{Genomic region to display (e.g., "chr1:1000000-2000000")}

\item{track_by}{Column name in data for separating tracks into facets (optional)}

\item{group_by}{Column name in data for grouping overlapping tracks (optional)}

\item{color_by}{Column name in data for coloring tracks (defaults to group_by if not specified)}

\item{colors}{Vector of colors for different groups (default: NULL, uses default ggplot2 colors)}

\item{type}{Type of signal visualization: "line", "area", or "heatmap" (default: "area")}

\item{alpha}{Transparency (default: 0.8)}

\item{binwidth}{Width of bins in base pairs (default: NULL)}

\item{facet_scales}{Scale parameter for facet_wrap (default: "free_y")}

\item{...}{Additional arguments passed to geom_signal}
}
\value{
A ggplot2 object
}
\description{
This function creates signal tracks from a data frame with grouping capabilities.
It can separate data into individual tracks using facets (\code{track_by}) and create
overlapping tracks within each facet using different colors (\code{group_by}).
}
\examples{
\dontrun{
# Basic usage with track separation
p <- plot_signal_df(signal_data, "chr1:1000000-2000000", track_by = "sample")

# With both track separation and grouping
p <- plot_signal_df(signal_data, "chr1:1000000-2000000",
  track_by = "sample", group_by = "condition"
)
}
}
