% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{df_to_granges}
\alias{df_to_granges}
\title{Convert a data frame to a GRanges object}
\usage{
df_to_granges(
  df,
  seqnames = "seqnames",
  start = "start",
  end = "end",
  strand = "strand"
)
}
\arguments{
\item{df}{A data frame with at least seqnames, start, and end columns}

\item{seqnames}{Column name for chromosome (default: "seqnames")}

\item{start}{Column name for start position (default: "start")}

\item{end}{Column name for end position (default: "end")}

\item{strand}{Column name for strand (default: "strand")}
}
\value{
A GRanges object
}
\description{
This function converts a data frame to a GRanges object. The data frame must have
columns for chromosome (seqnames), start, and end positions.
}
\examples{
\dontrun{
df <- data.frame(
  seqnames = c("chr1", "chr1", "chr2"),
  start = c(1, 100, 200),
  end = c(50, 150, 250),
  score = c(0.1, 0.5, 0.9)
)
gr <- df_to_granges(df)
}
}
